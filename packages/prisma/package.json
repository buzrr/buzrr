{
  "name": "@buzrr/prisma",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate": "prisma generate",
    "postinstall": "prisma generate",
    "build": "prisma generate && tsc",
    "check-types": "tsc --noEmit",
    "seed:duel": "node scripts/seed-duel-starter.mjs"
  },
  "files": [
    "dist",
    "generated"
  ],
  "dependencies": {
    "@prisma/adapter-pg": "^7.4.0",
    "@prisma/client": "^7.4.0",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "prisma": "^7.4.0",
    "typescript": "5.8.2"
  }
}
